import { IAuthClient, IStorage } from '@8base/utils';
import { AUTH_STRATEGIES } from './constants';
interface IAuthClientCreateOptions {
    strategy: AUTH_STRATEGIES | string;
    storage?: IStorage;
    storageKey?: string;
    subscribable?: boolean;
}
export declare class Auth {
    static createClient(options: IAuthClientCreateOptions, clientOptions: any): IAuthClient;
}
export {};
//# sourceMappingURL=Auth.d.ts.map